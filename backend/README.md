# backend/

Phase 2 (Backend Foundation) — not yet started. See `../PROJECT_STATUS.md`.

Planned layout: `app/api` (routers), `app/core` (settings, security helpers), `app/models` (SQLAlchemy models), `app/schemas` (Pydantic schemas), `app/services/{cv,ml,simulation}` (the actual pipeline logic), `app/db` (session/engine), `app/config` (runtime-tunable config loading).

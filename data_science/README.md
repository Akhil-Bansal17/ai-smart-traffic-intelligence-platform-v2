# data_science/

Research/training code, kept separate from `backend/app/services/ml` (production inference). See ARCHITECTURE.md §5.

- `notebooks/` — EDA, feature engineering exploration, model comparison.
- `scripts/` — training/eval scripts promoted out of notebooks once stable.
- `models/` — serialized model artifacts (git-ignored; not committed).

Not started — see `../PROJECT_STATUS.md`.

# tests/integration/

End-to-end pipeline tests: upload → process → generate metrics → store results → retrieve results → display dashboard. See ARCHITECTURE.md and Phase 17 in `../../PROJECT_STATUS.md`. Empty for now — no code exists yet to test.
